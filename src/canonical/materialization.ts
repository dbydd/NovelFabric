// Removed after V4 scope reset.
// Semantic workspace resources must be produced through constrained model/user proposals
// and applied via shared services, not synthesized by deterministic core TypeScript.
export {};
